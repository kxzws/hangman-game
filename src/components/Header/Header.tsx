import React from 'react';

import * as styled from './styled';

export const Header = () => {
  return (
    <styled.Header>
      <styled.Heading>Hangman Game</styled.Heading>
    </styled.Header>
  );
};
